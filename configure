#!/bin/sh
srcdir=$(cd $(dirname $0); pwd -P)

cat > Makefile <<EOF
srcdir:=$srcdir

default:
	\$(MAKE) -f \$(srcdir)/Makefile srcdir=\$(srcdir)

%:
	\$(MAKE) -f \$(srcdir)/Makefile srcdir=\$(srcdir) \$*
EOF
